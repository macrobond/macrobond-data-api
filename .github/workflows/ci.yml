
name: Continuous Integration

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  ci:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Set up Python 3.7
      uses: actions/setup-python@v3
      with:
        python-version: '3.7'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip

    - name: Mypy
      run: scripts/mypy.py

    - name: Pylint
      run: scripts/pylint.py

    - name: Pycodestyle
      run: scripts/pycodestyle.py

    - name: Pdoc3
      run: scripts/pdoc3.py
    
    - name: Build
      run: scripts/build_tool.py
